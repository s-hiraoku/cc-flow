# Security settings
package-lock=true
audit=true
audit-level=moderate

# Provenance (requires CI/CD)
# provenance=true

# Prevent accidental publish to wrong registry
registry=https://registry.npmjs.org/
